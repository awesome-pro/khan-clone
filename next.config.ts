import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "cdn.kastatic.org", "content.r9cdn.net", "cdn.shopify.com", "cdn0.desidime.com" ],
  },
};

export default nextConfig;
